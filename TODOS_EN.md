# TODOS — MarkdownViewer

Legend: `[ ]` to do · `[~]` in progress · `[x]` done · `[-]` cancelled

## v1 — Viewer MVP (shipped)

### Must-have
- [x] Bootstrap a SwiftUI Xcode project (target macOS 13+) — via XcodeGen
- [x] Wire `.md` / `.markdown` file association in `Info.plist` (`CFBundleDocumentTypes` + `UTImportedTypeDeclarations`)
- [x] `DocumentGroup` + `FileDocument` architecture — multi-window and Open Recent come for free
- [x] `WKWebView` embedded in a SwiftUI view via `NSViewRepresentable`
- [x] Local web bundle: `index.html` + markdown-it + highlight.js + KaTeX + Mermaid + DOMPurify + GitHub CSS
- [x] Render pipeline: Swift reads file → injects content via `evaluateJavaScript` → markdown-it parses → DOM rendered
- [x] Auto dark mode (CSS `prefers-color-scheme` + injected `data-theme` synced with `NSApp.effectiveAppearance`)
- [x] Native text selection / copy from `WKWebView`

### Validated features
- [x] Live reload: `DispatchSource` file watcher, 120 ms debounce, atomic-save handling, ~130 ms latency
- [x] Recent files (via `DocumentGroup`)
- [x] Find in document: `Cmd+F` floating `FindBar`, `Cmd+G` / `Cmd+Shift+G`, native `WKWebView.find`
- [x] Print / Export PDF: `Cmd+P` via native `WKWebView.printOperation`

### v1 polish
- [x] App icon — "M↓" indigo squircle generated by `Scripts/make-icon.swift`
- [ ] Stress-test on real files: large repo READMEs, 1 MB document, document with 100 images
- [x] Signing — superseded: releases are Developer ID signed + Hardened Runtime + notarized since v0.5.1
- [x] Runtime rendering test (math, code highlight, mermaid, dark mode)
- [x] `NSLog` → `os.Logger` (subsystem `com.vincent.MarkdownViewer`)
- [x] Set as default `.md` handler via `duti` (also `markdown`, `mdown`, `mdwn`, `mkd`, `mkdn`)

## v2 — Editing (shipped 2026-05-02)

- [x] Preview / Split / Source toggle (toolbar Picker, `Cmd+/` cycles)
- [x] Split mode via `HSplitView` (draggable divider)
- [x] Native `MarkdownEditor` (NSTextView wrapper) with regex-based Markdown syntax highlighting
- [x] Native `Cmd+S` / auto-save / dirty indicator / undo-redo via `FileDocument` + `NSTextView`
- [x] `CFBundleTypeRole`: Viewer → Editor
- [x] YAML frontmatter detection + toggle (`⇧⌘Y`, persisted per window, Obsidian-style hidden by default)

## v3 — Multi-platform (phases A–D shipped 2026-05-02)

- [x] **Phase A** — cross-platform refactor (`#if os(macOS|iOS)`), shared sources, NS/UIViewRepresentable duals
- [x] **Phase B** — `MarkdownViewerIOS` target (iOS 16+, iPhone + iPad, document browser support)
- [x] **Phase C** — asset catalog extended with the iOS marketing icon
- [x] **Phase D** — iOS Simulator build green (`CODE_SIGNING_ALLOWED=NO` required on CLI)
- [ ] **Phase E** — runtime smoke test on iPhone/iPad Simulator from the Xcode IDE
- [ ] **Phase F** — App Store distribution (signing, sandbox audit, review) — deferred
- [ ] Files.app + iCloud Drive compatibility test (with Phase E)
- [ ] Live reload on iPadOS via `NSFilePresenter` — backlog, if demand shows up

## v0.4 → v0.7 (shipped)

- [x] **v0.4** — home-grown update checker (GitHub Releases API) + `Scripts/release.sh` DMG pipeline
- [x] **v0.5** — line-numbers gutter (`NSRulerView`, soft-wrap-aware, dynamic width)
- [x] **v0.5.1** — notarized releases (Developer ID + Hardened Runtime + stapler)
- [x] **v0.6.0** — Sparkle 2 auto-update (EdDSA + appcast) + in-app Help / What's New / About windows; home-grown `UpdateChecker.swift` removed
- [x] **v0.6.1** — friendly DMG installer layout + appcast parse fix
- [x] **v0.6.2** — Sparkle prompts before downloading (no silent installs)
- [x] **v0.7.0** — content zoom (`⌘+` / `⌘-` / `⌘0`, per window) + current-line highlight in the gutter; `sparkle:version` now uses `CFBundleVersion`

## v0.8 — Quick Look + robustness (shipped: v0.8.0 on 2026-05-05, v0.8.1 on 2026-06-14)

- [x] **Quick Look extension** (`MarkdownViewerQL`) — spacebar preview in Finder, same web pipeline, sandboxed read-only
- [x] **Trimmed What's New window** — shows only the latest release notes (`/releases/latest`)
- [x] **Crash workaround (v0.8.1)** — window pinned to sRGB 8-bit to dodge missing half-float Metal shaders in macOS QuartzCore (Apple Silicon M4, macOS 26.5.1 / 27.0 beta)
- [ ] Submit the crash report to Apple via Feedback Assistant

## Backlog

- [x] Theme picker (Auto / GitHub Light / GitHub Dark / Sepia) — shipped 2026-07-02: 🎨 toolbar menu, persisted per window, forced via the web view's appearance, sepia palette through CSS custom properties. Future: user-defined themes.
- [ ] Floating / sidebar table of contents
- [ ] App Store distribution (signing + sandbox + review process)
- [ ] Migrate `highlight.js` → Shiki if syntax-highlighting quality becomes a bottleneck

## Tech debt & quality (identified 2026-07-02, foundation shipped 2026-07-02)

- [x] First automated tests — `MarkdownViewerTests` target (hosted), 25 tests across 3 suites: `WebPipelineTests`, `HighlighterTests`, `FileWatcherTests`
- [x] Deduplicate code shared between the app and the QL extension — `Sources/Shared/WebPipeline.swift`, compiled into all three targets
- [x] Disable `developerExtrasEnabled` (private KVC) outside Debug builds
- [x] Remove dead code (`ViewMode.cycled()` — would also have been wrong on iPhone)
- [x] Centralise the dark background color on the Swift side (`WebPipeline.darkBackground`)
- [ ] Centralise hardcoded GitHub URLs (HelpWindows + release.sh)
- [x] Surface `evaluateJavaScript` errors through `os.Logger` (app + QL) and `console.error` in render.js catches
- [x] Drop the private `drawsBackground` KVC in the QL extension (aligned with the crash workaround)
- [x] CI on GitHub Actions — `.github/workflows/ci.yml`: build + tests on macOS, iOS Simulator build
